import React from 'react';
const BottomText = (props) => {

    return(
        <React.Fragment>
            <footer>
                <p>Test</p>
            </footer>
        </React.Fragment>
    );
}

export default BottomText;